{
  "name": "rescue-onboarding",
  "version": "1.0.0",
  "private": true,
  "main": "App.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web",
    "update-app": "cd app && git pull origin main"
  },
  "dependencies": {
    "rescue": "../app"
  },
  "devDependencies": {
    "@babel/core": "^7.24.0",
    "prettier": "^3.2.5"
  }
}
